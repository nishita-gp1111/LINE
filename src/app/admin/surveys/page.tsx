"use client";
/* eslint-disable react-hooks/set-state-in-effect */
import Link from "next/link"; import { useEffect, useState } from "react";
type Survey = { id: string; name: string; status: string; question: { title: string; options: Array<{ key: string; label: string; token: string }> } };
export default function SurveysPage() { const [surveys, setSurveys] = useState<Survey[]>([]); const [form, setForm] = useState({ name: "", title: "", options: "はい,いいえ" }); const [message, setMessage] = useState(""); async function load() { const data = await (await fetch("/api/milestone3/interactive?resource=surveys")).json() as { surveys?: Survey[] }; setSurveys(data.surveys ?? []); } useEffect(() => { void load(); }, []); async function create() { const options = form.options.split(",").map((label, index) => ({ key: `option_${index + 1}`, label: label.trim() })).filter((item) => item.label); const data = await (await fetch("/api/milestone3/interactive", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ action: "survey_create", name: form.name, questionTitle: form.title, type: "single_choice", options }) })).json() as { error?: string }; setMessage(data.error || "アンケートをMock作成しました。"); if (!data.error) { setForm({ name: "", title: "", options: "はい,いいえ" }); await load(); } } return <main className="min-h-screen px-6 py-8 sm:px-10"><div className="mx-auto max-w-5xl"><Link href="/admin" className="text-sm text-moss">← 管理画面</Link><h1 className="mt-5 text-3xl font-black">アンケート</h1><section className="mt-6 rounded-xl border border-line bg-white p-6"><div className="grid gap-2"><input value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} placeholder="アンケート名" className="rounded border border-line px-3 py-2 text-sm" /><input value={form.title} onChange={(event) => setForm({ ...form, title: event.target.value })} placeholder="質問" className="rounded border border-line px-3 py-2 text-sm" /><input value={form.options} onChange={(event) => setForm({ ...form, options: event.target.value })} placeholder="選択肢をカンマ区切り" className="rounded border border-line px-3 py-2 text-sm" /><button onClick={() => void create()} disabled={!form.name || !form.title} className="rounded bg-ink px-3 py-2 text-sm font-bold text-white disabled:opacity-40">作成</button></div>{message ? <p className="mt-3 text-sm text-moss">{message}</p> : null}</section><section className="mt-6 rounded-xl border border-line bg-white p-6"><h2 className="font-black">一覧 / postback token状態</h2>{surveys.map((survey) => <div key={survey.id} className="mt-2 rounded border border-line p-3 text-sm"><p className="font-bold">{survey.name} / {survey.status}</p><p className="mt-1">{survey.question.title}: {survey.question.options.map((option) => option.label).join(", ")}</p></div>)}</section></div></main>; }
