import Link from "next/link";
import { publicLaunchStatus } from "@/lib/launch/cron";
export default function SchedulerPage() { const status = publicLaunchStatus(); return <main className="min-h-screen px-6 py-8 sm:px-10"><div className="mx-auto max-w-5xl"><Link href="/admin" className="text-sm text-moss">← 管理画面</Link><h1 className="mt-5 text-3xl font-black">Scheduler</h1><div className="mt-6 rounded-xl border border-line bg-white p-6"><p className="text-xs font-bold text-ink/50">Provider</p><p className="mt-2 text-xl font-black">{status.provider}</p><p className="mt-4 text-sm text-ink/65">CRON_SECRET: {status.cronSecretConfigured ? "設定済み" : "未設定"} / stale threshold: {status.staleAfterMinutes}分</p><p className="mt-4 text-sm text-ink/65">Supabase Cronの本番設定はVaultとProduction URLを人間が入力してから有効化します。</p></div></div></main>; }
