# Analytics

API受付と到達・既読を混同しません。dashboardは期間制限とdaily rollupを前提に、友だち状態、inbound、LINE受付、campaign、survey、click、conversionを表示します。取得不可は0でなく「データなし」。同一contactのunique countを基準にし、deterministic/estimated、集計日時、データ鮮度を分けます。
